const multiplier = 3;
let number = 0;

const lentele = `
${multiplier} + ${++number} = ${multiplier * number}
${multiplier} + ${++number} = ${multiplier * number}
${multiplier} + ${++number} = ${multiplier * number}
${multiplier} + ${++number} = ${multiplier * number}
${multiplier} + ${++number} = ${multiplier * number}
${multiplier} + ${++number} = ${multiplier * number}
`;

export default lentele;